# Tides

Experimental RuneLite plugin for prototyping water geometry.

This sample does not replace native water tiles. It renders a subdivided animated mesh over a configurable world area to demonstrate how sub-tile vertical displacement can look.
